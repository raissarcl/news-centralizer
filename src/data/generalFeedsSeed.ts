/**
 * Public seed for the general-news space.
 * When `defaultGeneralFeedsOpml.local.ts` exists, Metro resolves the import
 * below to that file so local builds use the full private catalog.
 */
export { DEFAULT_GENERAL_FEEDS_OPML } from './defaultGeneralFeedsOpml';
